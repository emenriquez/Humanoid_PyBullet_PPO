from setuptools import setup

setup(
    name="Humanoid-Basic-Env",
    version='0.0.1',
    install_requires=['gym',
                      'pybullet',
                      'numpy',
                      'matplotlib',
                      'torch']
)
